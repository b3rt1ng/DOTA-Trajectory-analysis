Pour lancer l'extraction, executer le script exec.sh

Il est possible de modifier les paramètres de l'extraction en modifiant directement le script exec.sh

Pour visualiser les résultats, on peut executer le script python visuSeq.py <numéro de la séquence à visualiser>
Note: on commence à 0